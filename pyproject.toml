[project]
name = "dbt-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Piyush Tech", email = "prajapatipiyush1209@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-fabric>=1.10.0",
]

[project.scripts]
dbt-tutorial = "dbt_tutorial:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
